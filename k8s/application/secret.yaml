# ⚠️ MANIFESTO DA FASE 2 (Minikube) — valores de dev em texto plano. O Bloco 4c substitui:
#   - banco: de /fase3/rds/* (senha regerada a cada destroy+apply do repo 3 → recriar do SSM
#     em TODO deploy, não uma vez só);
#   - JWT: /fase3/jwt/{private-key,public-key} montados como VOLUME, não env var. A chave
#     deixou de ir dentro da imagem no Bloco 4a (F9).
apiVersion: v1
kind: Secret
metadata:
  name: car-workshop-api-secret
  namespace: car-workshop
  labels:
    app: car-workshop-api
type: Opaque
stringData:
  QUARKUS_DATASOURCE_USERNAME: postgres
  QUARKUS_DATASOURCE_PASSWORD: postgres
  SECRET_KEY: oficina-secret-key-dev
